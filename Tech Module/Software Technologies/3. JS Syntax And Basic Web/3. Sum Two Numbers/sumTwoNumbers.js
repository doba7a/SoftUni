function sumTwoNumbers(arrayOfNumbers) {
    let firstNumber = Number(arrayOfNumbers[0]);
    let secondNumber = Number(arrayOfNumbers[1]);

    let sum = firstNumber + secondNumber;

    return sum;
}